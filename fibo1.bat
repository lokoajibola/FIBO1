@echo off
cd "C:\Users\MY PC\bets"  // Navigate to the script directory
python XU-FIBO-FT.py      // Execute the script
pause                      // Keeps the Command Prompt window open after execution